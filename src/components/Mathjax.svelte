<script>
    // import Mathjax from "mathjax";
    export let equation = `f(a) = \\frac{1}{2\\pi i} \\cint_\\gamma \\frac{f(z)}{z-a} dz`;
    const math = `$$` + equation + `$$`;
</script>


<div>
    <!-- { math } -->
    $$\\sin(x)$$
</div>
